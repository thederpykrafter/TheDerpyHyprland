# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# NOTE: Comments at end of line are neeeded for rofi keybinds script

# SUPER key
$mainMod = SUPER

# Unbind existing maps
unbind = $mainMod, M # Prevent accidentally exiting entirely

# Reload configs
bind = $mainMod SHIFT, R, exec, hyprctl reload # Reload configs

################
### PROGRAMS ###
################
bind = $mainMod, RETURN, exec, footclient # Open terminal
bind = $mainMod, N, exec, thunar # Open filemanager
bind = $mainMod, B, exec, firefox # Open browser
bind = $mainMod SHIFT, B, exec, firefox --private-window # Open private browser
bind = $mainMod SHIFT, K, exec, ~/.config/hypr/scripts/keybindings.sh # Show keybinds in rofi
bind = $mainMod, D, exec, rofi -show drun # Open rofi
bind = $mainMod, O, exec, obsidian # Open Obsidian Notes

# Actions
bind = $mainMod, Q, killactive # Close current window
bind = $mainMod SHIFT, E, exec, wlogout -b 5 -s # Power menu [lock,exit,reboot,suspend,shutdown]
bind = $mainMod, F, fullscreen # Open the window in fullscreen
bind = $mainMod SHIFT, F, togglefloating # Toggle tiling/floating
bind = $mainMod, G, exec, ~/.config/hypr/scripts/gamemode.sh # Toggle game mode
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, T, togglesplit, # dwindle
bind = $mainMod SHIFT, T, exec, ~/.config/hypr/scripts/titty-time.sh # Titty Time! (.) (.)

##################
### MEDIA KEYS ###
##################

# Volume
bindle =, XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.4 @DEFAULT_AUDIO_SINK@ 5%+ # Raise volume
bindle =, XF86AudioLowerVolume, exec, wpctl set-volume -l 1.4 @DEFAULT_AUDIO_SINK@ 5%- # Lower volume

# Brightness
bindle =, XF86MonBrightnessUp, exec, brightnessctl set 10%+ # Raise brightness
bindle =, XF86MonBrightnessDown, exec, brightnessctl set 10%- # Lower Brightness

# Microphone
bindl =, XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle # Toggle mute microphone sink
bindl =, XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle # Toggle mute microphone source

# Media controls
bindl=, XF86AudioPlay, exec, playerctl play-pause # Play/Pause audio
bindl=, XF86AudioStop, exec, playerctl stop # Play/Pause audio
bindl=, XF86AudioNext, exec, playerctl next # Next audio
bindl=, XF86AudioPrev, exec, playerctl previous # Previous Audio

# Wifi
bind =, XF86WLAN, exec, nmcli radio wifi toggle # Toggle wifi

##################
### WORKSPACES ###
##################

## FOCUS ##
# move focus with mainMod + <hjkl>
bind = $mainMod, H, movefocus, l # move focus left
bind = $mainMod, L, movefocus, r # move focus right
bind = $mainMod, K, movefocus, u # move focus up
bind = $mainMod, J, movefocus, d # move focus down

# move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l # move focus left
bind = $mainMod, right, movefocus, r # move focus right
bind = $mainMod, up, movefocus, u # move focus up
bind = $mainMod, down, movefocus, d # move focus down

## MOVE ##
# move window with mainMod SHIFT + <hjkl>
bind = $mainMod SHIFT, H, movewindow, l # move window left
bind = $mainMod SHIFT, L, movewindow, r # move window right
bind = $mainMod SHIFT, K, movewindow, u # move window up
bind = $mainMod SHIFT, J, movewindow, d # move window down

# move window with mainMod SHIFT + arrow keys
bind = $mainMod SHIFT, left, movewindow, l # move window left
bind = $mainMod SHIFT, right, movewindow, r # move window right
bind = $mainMod SHIFT, up, movewindow, u # move window up
bind = $mainMod SHIFT, down, movewindow, d # move window down

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1 # Switch to workspace 1
bind = $mainMod, 2, workspace, 2 # Switch to workspace 2
bind = $mainMod, 3, workspace, 3 # Switch to workspace 3
bind = $mainMod, 4, workspace, 4 # Switch to workspace 4
bind = $mainMod, 5, workspace, 5 # Switch to workspace 5
bind = $mainMod, 6, workspace, 6 # Switch to workspace 6
bind = $mainMod, 7, workspace, 7 # Switch to workspace 7
bind = $mainMod, 8, workspace, 8 # Switch to workspace 8
bind = $mainMod, 9, workspace, 9 # Switch to workspace 9
bind = $mainMod, 0, workspace, 10 # Switch to workspace 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1 #  Move window to workspace 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2 #  Move window to workspace 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3 #  Move window to workspace 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4 #  Move window to workspace 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5 #  Move window to workspace 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6 #  Move window to workspace 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7 #  Move window to workspace 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8 #  Move window to workspace 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9 #  Move window to workspace 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10 #  Move window to workspace 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1 # Scroll workspaces 
bind = $mainMod, mouse_up, workspace, e-1 # Scroll workspaces

###############
### WINDOWS ###
###############

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow # Move window
bindm = $mainMod, mouse:273, resizewindow # Resize window
